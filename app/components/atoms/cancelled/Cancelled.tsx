import {StyleSheet, Text, View} from 'react-native';
import React from 'react';
import NoticeList from '../NoticeList/NoticeList';

const Cancelled = () => {
  return <NoticeList point="Cancelled" />;
};

export default Cancelled;

const styles = StyleSheet.create({});
