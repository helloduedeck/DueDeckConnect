import {StyleSheet, Text, View} from 'react-native';
import React from 'react';
import NoticeList from '../NoticeList/NoticeList';
const Pending = () => {
  return <NoticeList point="Pending" />;
};

export default Pending;

const styles = StyleSheet.create({});
