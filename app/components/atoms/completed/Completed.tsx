import {StyleSheet, Text, View} from 'react-native';
import React from 'react';
import NoticeList from '../NoticeList/NoticeList';

const Completed = () => {
  return <NoticeList point="Completed" />;
};

export default Completed;

const styles = StyleSheet.create({});
