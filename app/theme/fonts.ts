const family = 'Roboto-';

const fonts = {
  ExtraBold: `${family}Black`,
  Bold: `${family}Bold`,
  SemiBold: `${family}Medium`,
  Normal: `${family}Regular`,
  Thin: `${family}Thin`,
  Light: `${family}Light`,
};
export default fonts;
